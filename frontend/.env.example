# Your FSU-1X consumer API key (generated via POST /v1/keys)
VITE_FSU_API_KEY=fsu1x_your_key_here

# Backend URL — Cloud Run service URL in prod, localhost:8000 in dev
VITE_API_BASE_URL=https://your-cloud-run-url.run.app
